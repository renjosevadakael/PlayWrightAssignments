/*Assignment Details:  
Declare variables using `let` for different data types in JavaScript, and verify their types
 using `typeof`, including an uninitialized variable. 
 
Assignment Requirements:  
    Create the following variables using let (not using var) and check their typeOf 
    a) firstName 
    b) companyName 
    c) mobileNumber 
    d) isAutomation 
    e) hasPlaywright (do not assign) 
 Print and confirm the values and data types */

 let firstName=`Renju`;
 let companyName='Cognizant';
 let mobileNumber=7560829071;
 let isAutomation=true;
let hasPlaywright;
console.log(`firstName---` +firstName);
console.log(`firstName typeOf---` +typeof firstName);
console.log(`companyName---` +companyName);
console.log(`companyName typeOf---` +typeof companyName);
console.log(`mobileNumber---` +mobileNumber);
console.log(`mobileNumber typeOf---` +typeof mobileNumber);
console.log(`isAutomation---` +isAutomation);
console.log(`isAutomation typeOf---` +typeof isAutomation);
console.log(`hasPlaywright---` +hasPlaywright);
console.log(`hasPlaywright typeOf---` +typeof hasPlaywright);
